load "resources/checkpoint1/data/*.yaml"  
    + select
        + fields "FILENAME"
        + from "params"
            + fields "criterion" "splitter" "ccp_alpha" "min_samples_split"
        * from
    * select
    + rename "FILENAME" to "File"
    + rename "criterion" to "Criterion"
    + rename "splitter" to "Splitter"
    + rename "ccp_alpha" to "CCP Alpha"
    + rename "min_samples_split" to "Min Samples Split"
save "Assignment 1.csv" 